/* Sample packet */

/* Initial request */
packet ChatRequest {
  // friend list
  vector<string> friends; //foo
  string name;
  unsigned int age;
  //Ipv4 addr;
  list<long double> stuff;
  time_t timestamp;
}

packet ChatResponse {
  short status;
  string name;
  map<string, vector<int> > stuff;
  size_t size;
  list<pair<int, string> > pairs;
}
